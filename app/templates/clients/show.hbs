<p>id: {{model.id}}</p>
<p>Name: {{model.name}}</p>
<p>{{link-to "Projects" "clients.show.projects" model.id}}</p>
{{outlet}}

<button class="btn" {{action 'edit'}}>Edit</button>
<button class="btn btn-danger" {{action 'delete'}}>Delete</button>
